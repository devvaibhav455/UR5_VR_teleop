settings:
  ask_to_execute: True
  re_act_before_exec: False

ros:
  loop_rate: 25 # Hz

topics:
  front_rgb: '/kinect_front/sd/image_color_rect'
  front_depth: '/kinect_front/sd/image_depth_rect'
  front_camera_info: '/kinect_front/sd/camera_info'

  joy_state: '/vive_right'
  joy_pose: '/right_controller_as_posestamped'

  joint_states: '/joint_states'
  target_pose: '/target_gripper'

  pred_voxel: '/peract_pred_voxel'

agent:
  seed_path: '/franka_logs/multi/FRANKA_C2FARM_PERCEIVER_LANG_BC/seed3/'
  weight: 22000

  episode_length: 10

  camera_resolution: [512, 424]
  scene_bounds: [0.0, -0.5, -0.1, 1.0, 0.5, 0.9]

  # rotation_resolution: 5
  # voxel_sizes: [100]
  # num_latents: 1024
  # latent_dim: 512

  # voxel_patch_size: 5
  # voxel_patch_stride: 5

  # lang_fusion_type: 'concat'

logs:
  save: False
  log_path: '/catkin_ws/src/dvnets_franka/franka_exec_logs'

  episode_id: 0
  task: 'stack_blocks'

  save_all_frames: False
  save_all_folder: 'ood_small_handsan'